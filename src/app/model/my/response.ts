import {ParagraphType} from '../view/ParagraphType';
import {ParapageType} from '../view/ParapageType';
import {ParatagType} from '../view/ParatagType';

export class Response {
  constructor(public message: string, public alert: string) {
  }
}
